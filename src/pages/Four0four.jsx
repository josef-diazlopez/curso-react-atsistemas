import React from "react";
import PropTypes from 'prop-types';

export const Four0four = ({ }) => {
    return (
        <div>
            Ups, parece que no hemos encontrado lo que buscas
        </div>
    )
}